<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title or "Home" }}</title>

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://annoyscript.vercel.app" />
    <meta property="og:title" content="AnnoyScript" />
    <meta
      property="og:description"
      content="Blog about the issues, annoyances, and hard-to-grasp concepts that I encountered while working with JavaScript and TypeScript"
    />
    <meta
      property="og:image"
      content="https://annoyscript.vercel.app/img/AnnoyScript_logo.svg"
    />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content="https://annoyscript.vercel.app" />
    <meta property="twitter:title" content="AnnoyScript" />
    <meta
      property="twitter:description"
      content="Blog about the issues, annoyances, and hard-to-grasp concepts that I encountered while working with JavaScript and TypeScript"
    />
    <meta
      property="twitter:image"
      content="https://annoyscript.vercel.app/img/AnnoyScript_logo.svg"
    />

    <!-- Meta Tags Generated with https://metatags.io -->
    <link rel="shortcut icon" type="image/jpg" href="/img/favicon.ico" />
    <meta
      name="description"
      content="{{ description or metadata.description }}"
    />
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}" />
    <link rel="stylesheet" href="{{ '/css/prism-vscode.dark.css' | url }}" />
    <link
      rel="alternate"
      href="{{ metadata.feed.path | url }}"
      type="application/atom+xml"
      title="{{ metadata.title }}"
    />
    <link
      rel="alternate"
      href="{{ metadata.jsonfeed.path | url }}"
      type="application/json"
      title="{{ metadata.title }}"
    />
  </head>
  <body>
    <header>
      {#- Read more about `eleventy-navigation` at
      https://www.11ty.dev/docs/plugins/navigation/ #}
      <ul class="nav">
        {%- for entry in collections.all | eleventyNavigation %}
        <li
          class="nav-item {% if entry.url == page.url %} nav-item-active{% endif %}"
        >
          <a href="{{ entry.url | url }}">{{ entry.title }}</a>
        </li>
        {%- endfor %}
      </ul>
      <h1 class="home">
        <img src="../../img/AnnoyScript_logo.svg" height="40" width="40" />
      </h1>
    </header>

    <main {% if templateClass %} class="{{ templateClass }}" {% endif %}>
      {{ content | safe }}
    </main>
    <footer>
      <div class="footer-text">
        © 2024 {{ metadata.title }}, All rights reserved
      </div>
    </footer>
  </body>
</html>
