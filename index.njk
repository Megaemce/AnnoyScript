---
layout: layouts/home.njk
eleventyNavigation:
  key: Posts
  order: 1
---

<h2>{{ metadata.description }}</h2>
{% set maxPosts = collections.posts.length | min(10) %} {#
<h3>
  Latest {% if maxPosts == 1 %}Post{% else %}{{ maxPosts }} Posts{% endif %}
</h3>
#} {% set postslist = collections.posts | head(-10) %} {% set postslistCounter =
collections.posts | length %} {% include "postslist.njk" %} {#
<p>
  More articles can be found in <a href="{{ '/posts/' | url }}">the archive</a>.
</p>
#}
